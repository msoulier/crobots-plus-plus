#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct main0_out
{
    float4 out_var_SV_Target [[color(0)]];
};

struct main0_in
{
    float2 in_var_TEXCOORD0 [[user(locn0)]];
};

fragment main0_out main0(main0_in in [[stage_in]], texture2d<float> PaletteTexture [[texture(0)]], sampler PaletteSampler [[sampler(0)]])
{
    main0_out out = {};
    out.out_var_SV_Target = PaletteTexture.sample(PaletteSampler, in.in_var_TEXCOORD0);
    return out;
}

